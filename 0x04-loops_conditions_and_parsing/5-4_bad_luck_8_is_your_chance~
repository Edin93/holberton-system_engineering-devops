#!/usr/bin/env bash
# Displays a specific output for each iteration.
i=1
while((i<11));do
    if ((i==4));then
	echo "bad luck"
    elif ((i==8));then
	echo "good luck"
    else
	echo "Holberton School"
    ((i++))
done
